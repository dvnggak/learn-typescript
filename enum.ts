enum gender {male = 1, female = 2, undefined = 0};

let jenisKelamin : gender = gender.male;
console.log(jenisKelamin);

// ENUM STRING
enum category {
    grocery = "GROCERY",
    electronic = "ELECTRONIC",
    handphone = "HANDPHONE"
};

let kategori : category = category.grocery;
console.log(kategori);